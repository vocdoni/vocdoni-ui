/** The keys below are the ones that Plaza will use within `metadata.meta = {...}` */
export enum PlazaMetadataKeys {
  BRAND_COLOR = "brandColor",
  SECONDARY_COLOR = "secondaryColor",
  DISCUSSION_URL = "discussionUrl",
  ATTACHMENT_URI = "attachmentUri"   // Can be an IPFS URI, not necessarily a URL
}
